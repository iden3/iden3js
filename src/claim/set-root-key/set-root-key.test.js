const chai = require('chai');
const SetRootKey = require('./set-root-key');
const utils = require('../../utils');

const { expect } = chai;

describe('[Claim set-id-root]', () => {
  it('Create claim setIdRoot', () => {
    const newClaim = new SetRootKey.SetRootKey();
  });
});
